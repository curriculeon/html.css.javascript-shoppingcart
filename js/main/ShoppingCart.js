class ShoppingCart {
    constructor() {
      // The list of products in the shopping cart
      this.products = [];
    }
  
    // Adds a product to the shopping cart
    // @param {Product} product - The product to add
    addProduct(product) {}
  
    // Removes a product from the shopping cart
    // @param {Product} product - The product to remove
    removeProduct(product) {}
  
    // Calculates the total cost of the products in the shopping cart
    // @return {number} - The total cost of the products
    calculateTotalCost() {}
  }
  